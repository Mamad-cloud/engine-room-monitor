# Engine Room Monitor — quick start (dev)

## Prereqs
- Docker & Docker Compose
- Node & npm (optional for local dev on node-bridge)
- Composer (optional if you run composer inside container)

## Setup & run

1. Build images:
```bash
docker compose build
